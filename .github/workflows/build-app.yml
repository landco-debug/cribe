name: Build Cribe — ASR-Whisper-HandyParity-GGUF-BIN-TranscribeCpp

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'Sources/**'
      - 'Tests/**'
      - 'Resources/**'
      - 'scripts/**'
      - 'Info.plist'
      - 'Dev.entitlements'
      - 'Package.swift'
      - 'Package.resolved'
      - '.github/workflows/build-app.yml'
  pull_request:
    paths:
      - 'Sources/**'
      - 'Tests/**'
      - 'Resources/**'
      - 'scripts/**'
      - 'Info.plist'
      - 'Dev.entitlements'
      - 'Package.swift'
      - 'Package.resolved'
      - '.github/workflows/build-app.yml'

concurrency:
  group: build-app-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Cribe — ASR-Whisper-HandyParity-GGUF-BIN-TranscribeCpp (Apple Silicon)
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Build app with official script
        run: bash scripts/build-app.sh

      - name: Verify app signature
        run: codesign --verify --deep --strict --verbose=2 dist/Cribe.app

      - name: Verify packaged runtime dependencies
        run: bash scripts/verify-app-runtime.sh dist/Cribe.app

      - name: Launch smoke-test packaged app
        run: bash scripts/verify-app-runtime.sh dist/Cribe.app --launch

      - name: Pack app
        run: ditto -c -k --sequesterRsrc --keepParent dist/Cribe.app Cribe-ASR-Whisper-HandyParity-GGUF-BIN-TranscribeCpp-macOS-Apple-Silicon.zip

      - name: Upload ready-to-install app
        uses: actions/upload-artifact@v4
        with:
          name: Cribe-ASR-Whisper-HandyParity-GGUF-BIN-TranscribeCpp-macOS-Apple-Silicon
          path: Cribe-ASR-Whisper-HandyParity-GGUF-BIN-TranscribeCpp-macOS-Apple-Silicon.zip
          if-no-files-found: error
          retention-days: 30
